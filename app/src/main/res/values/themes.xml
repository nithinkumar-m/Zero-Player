<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.ZeroPlayer" parent="Theme.Material3.DayNight">
        <!-- Customize if you want system bars styling -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorSurface</item>
        <item name="android:navigationBarColor" tools:targetApi="l">?attr/colorSurface</item>
    </style>

    <!-- Pre-Android 12 fallback splash theme -->
    <style name="Theme.ZeroPlayer.Splash" parent="Theme.ZeroPlayer">
        <item name="android:windowBackground">?attr/colorSurface</item>
    </style>

</resources>
